# 开发前必读

- [术语介绍](https://uat.rztcd.cn:89/api/doc#术语介绍)
- 开始
  - [第一步：创建单位应用](https://uat.rztcd.cn:89/api/doc#第一步：创建单位应用)
  - [第二步：开启接收消息模式](https://uat.rztcd.cn:89/api/doc#第二步：开启接收消息模式)
  - [第三步：获取access_token](https://uat.rztcd.cn:89/api/doc#第三步：获取access_token)
  - [第四步：对access_token进行缓存处理](https://uat.rztcd.cn:89/api/doc#第四步：对access_token进行缓存处理)
  - [第五步：开发应用逻辑](https://uat.rztcd.cn:89/api/doc#第五步：开发应用逻辑)
- 开发注意事项

## 术语介绍

- corpid：每个单位都拥有唯一的corpid，获取此信息可在管理后台“我的单位”－“单位信息”下查看（需要有管理员权限）。
- secret：secret是单位应用里面用于保障数据安全的“钥匙”，每一个应用都有一个独立的访问密钥，为了保证数据的安全。
- 目前secret有两种：1. 通讯录管理secret； 2. 应用secret。
  - 通讯录管理secret。在“管理工具”-“通讯录同步及内部登录集成”里面查看（需开启“API接口同步”）；
  - 应用secret。在管理后台->“单位应用”->点进应用，即可看到。
- access_token：access_token是单位后台去政务微信的后台获取信息时的重要票据，所有接口在通信时都需要携带此信息用于验证接口的访问权限。
- userid： 每个成员都有唯一的userid，即所谓“账号”。在管理后台->“通讯录”->点进某个成员的详情页，可以看到。
- 部门id： 每个部门都有唯一的id，在管理后台->“通讯录”->“组织架构”->点击某个部门右边的小圆点可以看到。
- tagid：每个标签都有唯一的标签id，在管理后台->“通讯录”->“标签”，选中某个标签，在右上角会有“标签详情”按钮，点击即可看到。

## 开始

### 第一步：创建单位应用

登录单位管理后台进入“单位应用”页面，通讯录管理是政务微信默认集成的应用，可以直接开启，如果单位需要开发自定义的应用，可点击“添加应用”完成应用的添加和配置。

### 第二步：开启接收消息模式

开启接收消息模式并不是必须步骤，但是如果在你的单位应用中需要用到如下功能时需提前开启接收消息模式

- 获取单位成员的地理位置信息
- 动态调整单位应用的信息
- 获取单位成员点击事件类型的应用菜单行为
- 获取单位成员通过应用给单位后台发送的消息

关于如何开启接收消息模式，请阅读[接收消息模式设置](https://uat.rztcd.cn:89/api/doc#10514)章节。

### 第三步：获取access_token

**请求方式：**GET
**请求URL：**https://uat.rztcd.cn/cgi-bin/gettoken?corpid=id&corpsecret=secrect

**参数说明：**

| 参数       | 必须 | 说明           |
| ---------- | ---- | -------------- |
| corpid     | 是   | 单位ID         |
| corpsecret | 是   | 应用的凭证密钥 |

**权限说明：**

每个应用有独立的secret，所以每个应用的access_token应该分开来获
自建应用需要配置[IP安全策略](https://uat.rztcd.cn:89/api/doc/#11381)才能获取aceess_token

**返回结果：**

```
{   "errcode":0，   "errmsg":"ok"，   "access_token": "accesstoken000001",   "expires_in": 7200}
```

| 参数         | 说明                                     |
| ------------ | ---------------------------------------- |
| errcode      | 出错返回码，为0表示成功，非0表示调用失败 |
| errmsg       | 返回码提示语                             |
| access_token | 获取到的凭证，最长为512字节              |
| expires_in   | 凭证的有效时间（秒）                     |

**出错返回示例**：

{
“errcode”:40091,
“errmsg”:”provider_secret is invalid”
}

**版本信息：**
v1.0以上版本有效

### 第四步：对access_token进行缓存处理

为了保障服务器的稳定性，防止高频率调用而影响功能正常使用，建议开发者将中间生成的access_token 进行缓存，过期以后再重新获取。同时由于政务微信每个应用的access_token是彼此独立的，所以进行缓存时需要区分应用来进行存储。

### 第五步：开发应用逻辑

到这里，准备工作已经就绪，你可以继续阅读下面的接口文档了解更多关于政务微信提供的开放能力，并将这些能力与你的应用进行结合，开始吧！

## 开发注意事项

如当前域名https://uat.rztcd.cn无法访问，可修改为接入机的地址进行访问。
文档中的接口，都进行了版本信息注释,非直接接口类模块，在模块标题处，注释模块版本信息。使用微信服务低版本用户，对注释的高版本接口调用无效，需升级到高版本方可有效调用。