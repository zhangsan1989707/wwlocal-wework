# 异步批量接口

- [异步批量接口说明](https://uat.rztcd.cn:89/api/doc#%E5%BC%82%E6%AD%A5%E6%89%B9%E9%87%8F%E6%8E%A5%E5%8F%A3%E8%AF%B4%E6%98%8E)

- [通讯录更新](https://uat.rztcd.cn:89/api/doc#%E9%80%9A%E8%AE%AF%E5%BD%95%E6%9B%B4%E6%96%B0)

    - [增量更新成员](https://uat.rztcd.cn:89/api/doc#%E5%A2%9E%E9%87%8F%E6%9B%B4%E6%96%B0%E6%88%90%E5%91%98)

    - [全量覆盖成员](https://uat.rztcd.cn:89/api/doc#%E5%85%A8%E9%87%8F%E8%A6%86%E7%9B%96%E6%88%90%E5%91%98)

    - [全量覆盖部门](https://uat.rztcd.cn:89/api/doc#%E5%85%A8%E9%87%8F%E8%A6%86%E7%9B%96%E9%83%A8%E9%97%A8)

    - [批量添加部门](https://uat.rztcd.cn:89/api/doc#%E6%89%B9%E9%87%8F%E6%B7%BB%E5%8A%A0%E9%83%A8%E9%97%A8)

    - [异步获取部门成员信息](https://uat.rztcd.cn:89/api/doc#%E5%BC%82%E6%AD%A5%E8%8E%B7%E5%8F%96%E9%83%A8%E9%97%A8%E6%88%90%E5%91%98%E4%BF%A1%E6%81%AF)

        - [接口说明](https://uat.rztcd.cn:89/api/doc#%E6%8E%A5%E5%8F%A3%E8%AF%B4%E6%98%8E)

        - [获取部门成员](https://uat.rztcd.cn:89/api/doc#%E8%8E%B7%E5%8F%96%E9%83%A8%E9%97%A8%E6%88%90%E5%91%98)

        - [获取部门成员详情](https://uat.rztcd.cn:89/api/doc#%E8%8E%B7%E5%8F%96%E9%83%A8%E9%97%A8%E6%88%90%E5%91%98%E8%AF%A6%E6%83%85)

- [获取异步任务结果](https://uat.rztcd.cn:89/api/doc#%E8%8E%B7%E5%8F%96%E5%BC%82%E6%AD%A5%E4%BB%BB%E5%8A%A1%E7%BB%93%E6%9E%9C)

- [附注：](https://uat.rztcd.cn:89/api/doc#%E9%99%84%E6%B3%A8%EF%BC%9A)

- [扩展属性的添加方法](https://uat.rztcd.cn:89/api/doc#%E6%89%A9%E5%B1%95%E5%B1%9E%E6%80%A7%E7%9A%84%E6%B7%BB%E5%8A%A0%E6%96%B9%E6%B3%95)

## 异步批量接口说明

异步批量接口用于大批量数据的处理，提交后接口即返回，政务微信会在后台继续执行任务。

执行完成，执行过程中用户可以调用结果查询接口，检查结果的执行状态，建议对结果查询调用的频率进行控制，可以设置2s执行一次查询操作。

目前，仅为通讯录更新提供了异步批量接口

**注意：** 该部分接口在1.3.5版本后有效

## 通讯录更新

通讯录更新接口提供四种更新方法：1) 增量更新成员 2）全量覆盖成员 3) 全量覆盖部门 4) 批量增加部门。后面具体描述更新逻辑。

如果单位要做到与政务微信通讯录完全一致，可先调用全量覆盖部门接口，再调用全量覆盖成员接口，即可保持通讯录完全一致。

**使用步骤：**

1.下载接口对应的csv模板，如果有扩展属性，请自行添加（扩展属性需要在WEB管理端创建后才生效，否则忽略未知属性的赋值。见创建扩展属性）

2.按模板的格式，生成接口所需的数据文件（必须保持为utf-8格式）

3.通过上传媒体文件接口上传数据文件，获取media_id

4.调用通讯录更新接口，传入media_id参数

5.调用批量查询接口，获取任务执行结果

### 增量更新成员

**接口说明：**

本接口以userid（账号）为主键，增量更新政务微信通讯录成员。请先下载CSV模板([下载增量更新成员模版](https://uat.rztcd.cn:89/wwopen/downloadfile/batch_user_sample.csv))，根据需求填写文件内容。

- 模板中的部门需填写部门ID，多个部门用分号分隔，部门ID必须为数字，根部门的部门id默认为1

- 文件中存在、通讯录中也存在的成员，更新成员在文件中指定的字段值

- 文件中存在、通讯录中不存在的成员，执行添加操作

- 通讯录中存在、文件中不存在的成员，保持不变

- 成员字段更新规则：可自行添加扩展字段。文件中有指定的字段，以指定的字段值为准；文件中没指定的字段，不更新

- 对于已经登录使用的账号，不会更新csv文件带的初始密码

- 别名、手机、邮箱、座机、职位、自定义字段可以通过空格清空数据

- 别名长度不超过64个字。当别名作为登录字段时，只能由字母、数字及`.-_@`组成，`.-_@`不能放在开头，且数据不能重复。

**请求方式** ：POST

**请求地址** ：https://uat.rztcd.cn/cgi-bin/batch/syncuser?access_token=ACCESS_TOKEN

**请求包体** ：

```json
{
    "media_id":"xxxxxx"
}
```

**参数说明** ：

| 参数 | 必须 | 说明 |
|---|---|---|
| access_token | 是 | 调用接口凭证 |
| media_id | 是 | 上传的csv文件的media_id |

**权限说明：**

只要通讯同步助手的凭证具有调用权限，且须拥有通讯录的写权限。

**返回结果：**

```json
{
   "errcode": 0,
   "errmsg": "ok",
   "jobid": "xxxxx"
}
```

**参数说明** ：

| 参数 | 说明 |
|---|---|
| errcode | 返回码 |
| errmsg | 对返回码的文本描述内容 |
| jobid | 异步任务id，最大长度为64字节, 可以通过获取结果的接口获取异步结果 |

### 全量覆盖成员

**接口说明** ：

本接口以userid为主键，全量覆盖单位的通讯录成员，任务完成后单位的通讯录成员与提交的文件完全保持一致。请先下载CSV文件([下载全量覆盖成员模版](https://uat.rztcd.cn:89/wwopen/downloadfile/batch_user_sample.csv))，根据需求填写文件内容。

**注意事项** ：

- 模板中的部门需填写部门ID，多个部门用分号分隔，部门ID必须为数字，根部门的部门id默认为1

- 文件中存在、通讯录中也存在的成员，完全以文件为准

- 文件中存在、通讯录中不存在的成员，执行添加操作

- 通讯录中存在、文件中不存在的成员，执行删除操作。出于安全考虑，下面两种情形系统将中止导入并返回相应的错误码。

    - 需要删除的成员多于50人，且多于现有人数的20%以上

    - 需要删除的成员少于50人，且多于现有人数的80%以上

- 成员字段更新规则：可自行添加扩展字段。文件中有指定的字段，以指定的字段值为准；文件中没指定的字段，不更新

- 对于已经登录使用的账号，不会更新csv文件带的初始密码

- 别名、手机、邮箱、座机、职位、自定义字段可以通过空格清空数据

- 别名长度不超过64个字。当别名作为登录字段时，只能由字母、数字及`.-_@`组成，`.-_@`不能放在开头，且数据不能重复。

**请求方式** ：POST

**请求地址** ：https://uat.rztcd.cn/cgi-bin/batch/replaceuser?access_token=ACCESS_TOKEN

**请求包体** ：

```json
{
    "media_id":"xxxxxx"
}
```

**参数说明** ：

| 参数 | 必须 | 说明 |
|---|---|---|
| access_token | 是 | 调用接口凭证 |
| media_id | 是 | 上传的csv文件的media_id |

**权限说明：**

只要通讯同步助手的凭证具有调用权限，且须拥有通讯录的写权限。

**返回结果：**

```json
{
   "errcode": 0,
   "errmsg": "ok",
   "jobid": "xxxxx"
}
```

**参数说明** ：

| 参数 | 说明 |
|---|---|
| errcode | 返回码 |
| errmsg | 对返回码的文本描述内容 |
| jobid | 异步任务id，最大长度为64字节, 可以通过获取结果的接口获取异步结果 |

### 全量覆盖部门

**接口说明** ：

本接口以partyid为键，全量覆盖单位的通讯录组织架构，任务完成后单位的通讯录组织架构与提交的文件完全保持一致。请先下载CSV文件([下载全量覆盖部门模版](https://uat.rztcd.cn:89/wwopen/downloadfile/batch_party_sample.csv))，根据需求填写文件内容。

**注意事项** ：

- 文件中存在、通讯录中也存在的部门，执行修改操作

- 文件中存在、通讯录中不存在的部门，执行添加操作

- 文件中不存在、通讯录中存在的部门，当部门下没有任何成员或子部门时，执行删除操作

- 文件中不存在、通讯录中存在的部门，当部门下仍有成员或子部门时，暂时不会删除，当下次导入成员把人从部门移出后自动删除

- CSV文件中，部门名称、部门ID、父部门ID为必填字段，部门ID必须为数字，根部门的部门id默认为1；排序为可选字段，置空或填0不修改排序, order值大的排序靠前。 order值相等的情况下，按照部门id排，id越小排序越靠前

- 部门详情中的单位简介、单位网站、单位地址、单位电话、社会信用代码、单位管理员字段可以通过空格清空数据。

**请求方式** ：POST

**请求地址** ：https://uat.rztcd.cn/cgi-bin/batch/replaceparty?access_token=ACCESS_TOKEN

**请求包体** ：

```json
{
    "media_id":"xxxxxx"
}
```

**参数说明** ：

| 参数 | 必须 | 说明 |
|---|---|---|
| access_token | 是 | 调用接口凭证 |
| media_id | 是 | 上传的csv文件的media_id |

**权限说明：**

只要通讯同步助手的凭证具有调用权限，且须拥有通讯录的写权限。

**返回结果：**

```json
{
   "errcode": 0,
   "errmsg": "ok",
   "jobid": "xxxxx"
}
```

**参数说明** ：

| 参数 | 说明 |
|---|---|
| errcode | 返回码 |
| errmsg | 对返回码的文本描述内容 |
| jobid | 异步任务id，最大长度为64字节, 可以通过获取结果的接口获取异步结果 |

### 批量添加部门

**接口说明：**

批量添加政务微信通讯录部门。请先下载CSV模板([下载增量更新部门模版](https://uat.rztcd.cn:89/wwopen/downloadfile/batch_add_party_sample.csv))，根据需求填写文件内容。

- 模板中的部门需填写部门路径，部门路径必须为从根部门开始的全路径

- 文件中存在、通讯录中不存在的部门，执行添加操作

- 部门详情中的单位简介、单位网站、单位地址、单位电话、社会信用代码、单位管理员字段可以通过空格清空数据。

**请求方式** ：POST

**请求地址** ：https://uat.rztcd.cn/cgi-bin/batch/syncparty?access_token=ACCESS_TOKEN

**请求包体** ：

```json
{
    "media_id":"xxxxxx",
}
```

**参数说明** ：

| 参数 | 必须 | 说明 |
|---|---|---|
| access_token | 是 | 调用接口凭证 |
| media_id | 是 | 上传的csv文件的media_id |

**权限说明：**

只要通讯同步助手的凭证具有调用权限，且须拥有通讯录的写权限。

**返回结果：**

```json
{
   "errcode": 0,
   "errmsg": "ok",
   "jobid": "xxxxx"
}
```

**参数说明** ：

| 参数 | 说明 |
|---|---|
| errcode | 返回码 |
| errmsg | 对返回码的文本描述内容 |
| jobid | 异步任务id，最大长度为64字节, 可以通过获取结果的接口获取异步结果 |

### 异步获取部门成员信息

#### 接口说明

本接口将返回一个jobid，通过jobid查询具体的数据。每5万的数据以json的格式写入到文件中。具体的json格式参考[获取部门成员](https://uat.rztcd.cn:89/api/doc#10061)和[获取部门成员详情](https://uat.rztcd.cn:89/api/doc#10063)。获取文件参考[获取临时素材文件](https://uat.rztcd.cn:89/api/doc#10115)。

#### 获取部门成员

**请求方式：** GET

**请求地址：** https://uat.rztcd.cn/cgi-bin/async/simplelist?access_token=ACCESS_TOKEN&department_id=DEPARTMENT_ID&fetch_child=FETCH_CHILD

**参数说明：**

| 参数 | 必须 | 说明 |
|---|---|---|
| access_token | 是 | 调用接口凭证 |
| department_id | 是 | 获取的部门id |
| fetch_child | 否 | 1/0：是否递归获取子部门下面的成员 |

**权限说明：**

应用须拥有指定部门的查看权限。

**返回结果：**

```json
{
    "errcode": 0,
    "errmsg": "ok",
    "jobid": "6_1597673058_231598"
}
```

**参数说明：**

| 参数 | 说明 |
|---|---|
| errcode | 返回码 |
| errmsg | 对返回码的文本描述内容 |
| jobid | 可以通过jobid查询结果，查询接口：[获取异步任务结果](https://uat.rztcd.cn:89/api/doc#10016/%E8%8E%B7%E5%8F%96%E5%BC%82%E6%AD%A5%E4%BB%BB%E5%8A%A1%E7%BB%93%E6%9E%9C) |

**版本信息：**

v2.5.0以上版本有效

#### 获取部门成员详情

**请求方式：** GET

**请求地址：** https://uat.rztcd.cn/cgi-bin/async/list?access_token=ACCESS_TOKEN&department_id=DEPARTMENT_ID&fetch_child=FETCH_CHILD

**参数说明：**

| 参数 | 必须 | 说明 |
|---|---|---|
| access_token | 是 | 调用接口凭证 |
| department_id | 是 | 获取的部门id |
| fetch_child | 否 | 1/0：是否递归获取子部门下面的成员 |

**权限说明：**

应用须拥有指定部门的查看权限。

**返回结果：**

```json
{
    "errcode": 0,
    "errmsg": "ok",
    "jobid": "6_1597673058_231598"
}
```

**参数说明：**

| 参数 | 说明 |
|---|---|
| errcode | 返回码 |
| errmsg | 对返回码的文本描述内容 |
| jobid | 可以通过jobid查询结果，查询接口：[获取异步任务结果](https://uat.rztcd.cn:89/api/doc#10016/%E8%8E%B7%E5%8F%96%E5%BC%82%E6%AD%A5%E4%BB%BB%E5%8A%A1%E7%BB%93%E6%9E%9C) |

**版本信息：**

v2.5.0以上版本有效

### 获取异步任务结果

**请求方式：** GET

**请求地址：** https://uat.rztcd.cn/cgi-bin/async/getresult?access_token=ACCESS_TOKEN&jobid=JOBID

**参数说明：**

| 参数 | 必须 | 说明 |
|---|---|---|
| access_token | 是 | 调用接口凭证 |
| jobid | 是 | 异步任务的jobid |

**权限说明：**

应用须拥有指定部门的查看权限。

**返回结果：**

```json
{
    "errcode": 0,
    "errmsg": "ok",
    "status": 3,
    "type": "sync_user",
    "detail": []
}
```

**参数说明：**

| 参数 | 说明 |
|---|---|
| errcode | 返回码 |
| errmsg | 对返回码的文本描述内容 |
| status | 任务状态，0-排队中 1-执行中 2-完成 3-失败 |
| type | 任务类型 |
| detail | 任务详情，status=3时返回 |

**版本信息：**

v2.5.0以上版本有效

### 附注：

#### 扩展属性的添加方法

在通讯录同步CSV模板中，对于扩展属性，请按以下方式填写：

- 扩展属性需要在WEB管理端创建后才生效，否则系统会忽略未知属性的赋值。

- 在CSV模板中，扩展属性列名请使用管理端创建时的属性名。

- 属性值请按实际需求填写，可使用空格清空数据。

### 扩展属性的添加方法

在通讯录同步CSV模板中，对于扩展属性，请按以下方式填写：

- 扩展属性需要在WEB管理端创建后才生效，否则系统会忽略未知属性的赋值。

- 在CSV模板中，扩展属性列名请使用管理端创建时的属性名。

- 属性值请按实际需求填写，可使用空格清空数据。

### 附注

#### 扩展属性的添加方法

在通讯录同步CSV模板中，对于扩展属性，请按以下方式填写：

- 扩展属性需要在WEB管理端创建后才生效，否则系统会忽略未知属性的赋值。

- 在CSV模板中，扩展属性列名请使用管理端创建时的属性名。

- 属性值请按实际需求填写，可使用空格清空数据。

### 通讯录更新接口CSV模板字段说明

#### 成员CSV模板字段（batch_user_sample.csv）

| 字段名 | 必须 | 说明 |
|---|---|---|
| userid | 是 | 成员账号 |
| name | 是 | 成员名称 |
| department | 是 | 部门ID，多个部门用分号分隔 |
| position | 否 | 职位信息 |
| mobile | 否 | 手机号码 |
| email | 否 | 邮箱 |
| telephone | 否 | 座机 |
| alias | 否 | 别名 |
| [扩展属性] | 否 | 自定义扩展字段 |

#### 部门CSV模板字段（batch_party_sample.csv）

| 字段名 | 必须 | 说明 |
|---|---|---|
| name | 是 | 部门名称 |
| partyid | 是 | 部门ID |
| parentid | 是 | 父部门ID，根部门为1 |
| order | 否 | 排序号 |

#### 部门CSV模板字段（batch_add_party_sample.csv）

| 字段名 | 必须 | 说明 |
|---|---|---|
| name | 是 | 部门名称 |
| path | 是 | 部门路径，从根部门开始的全路径 |
| [扩展属性] | 否 | 自定义扩展字段 |

### 常见问题

**Q: 异步任务执行失败如何处理？**

A: 请检查返回的errcode，根据错误码定位问题。常见错误包括：media_id无效、CSV格式错误、数据超出限制等。

**Q: 如何查询任务执行进度？**

A: 使用获取异步任务结果接口，通过jobid查询任务状态和详情。

**Q: 全量覆盖成员时误删了成员怎么办？**

A: 系统有安全保护机制，当删除成员数量超过限制时会拒绝执行。如需恢复，请联系管理员或使用备份数据重新导入。

**Q: CSV文件有什么格式要求？**

A: 文件必须保存为UTF-8编码，包含表头行，数据行按模板格式填写。
